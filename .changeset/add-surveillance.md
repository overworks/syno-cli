---
"@overworks/syno-cli": minor
---

Add `@overworks/syno-surveillance` and the `syno surveillance` command group:
`info`, `camera list`, `camera snapshot <id>` (JPEG), and `recording list` over
`SYNO.SurveillanceStation.*`.
