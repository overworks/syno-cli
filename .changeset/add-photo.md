---
"@overworks/syno-cli": minor
---

Add `@overworks/syno-photo` and the `syno photo` command group: `album list`,
`list` (items, with `--album`/`--folder`/`--type` filters), and `download <id>`
over `SYNO.Foto.*` (Synology Photos, Personal Space).
